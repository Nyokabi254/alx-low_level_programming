#include "main.h"
/**
 * mul - multiplies two integers
 * x - te first number to multiply
 * y - the second number to multiply
 * return - the result of the multiplication
 */
int mul(int x, int y)
{
	return (x * y);
}
